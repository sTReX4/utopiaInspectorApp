/* The field and primary-action classes were repeated verbatim across every
 * dialog on this page. Naming them once keeps the forms visually identical
 * and makes the next token change a single edit. */
export const FIELD =
  'w-full border border-line p-3 rounded-control outline-none focus:border-ink focus:ring-1 focus:ring-info-ink bg-surface text-sm font-medium text-ink';

export const SELECT = `${FIELD} cursor-pointer`;

export const PRIMARY_ACTION =
  'w-full bg-ink text-surface text-xs font-bold py-3 rounded-control hover:bg-shell-hover transition-colors duration-200';

export const LABEL = 'block text-xs font-bold text-ink-muted mb-2';
